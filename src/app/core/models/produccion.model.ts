export interface OrdenProduccion { id: string; producto: string; cantidad: number; estado: string; inicio: string; fin: string; }
export interface LineaProduccion { id: string; nombre: string; estado: string; eficiencia: number; }
