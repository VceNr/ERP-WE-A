export interface OrdenCompra { id: string; proveedor: string; fecha: string; total: number; estado: string; }
export interface Proveedor { id: string; nombre: string; contacto: string; email: string; categoria: string; }
